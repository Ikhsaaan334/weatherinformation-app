meta {
  name: 14 [user] Admin Users is forbidden
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/admin/users
  body: none
  auth: none
}

docs {
  Negative test: still logged in as the regular user. Listing users is admin-only
  -> expect 403.
}

tests {
  test("regular user is forbidden", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
