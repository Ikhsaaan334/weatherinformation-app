meta {
  name: 06 Cities - Delete (admin)
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/cities/{{newCityId}}
  body: none
  auth: none
}

docs {
  Admin-only. Deletes the city created in step 04 (keeps seeded cities intact).
}

tests {
  test("admin deletes the city", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().message).to.be.a('string');
  });
}
