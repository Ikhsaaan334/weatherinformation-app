meta {
  name: 03 Cities - List
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/cities?search=
  body: none
  auth: none
}

params:query {
  search:
}

tests {
  test("returns the Cities/Index component with can flags", function () {
    expect(res.getStatus()).to.equal(200);
    const props = res.getBody().props;
    expect(props).to.have.property('cities');
    expect(props.can).to.have.property('manage_cities');
  });
}
